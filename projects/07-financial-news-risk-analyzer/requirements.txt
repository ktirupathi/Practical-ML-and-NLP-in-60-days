# ── Core ML ──────────────────────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.38.0
tokenizers>=0.15.0
accelerate>=0.27.0
datasets>=2.17.0

# ── NLP / NER ─────────────────────────────────────────────────────────────────
# spacy is optional; used only for production NER (entity extraction falls back
# to regex heuristics if spacy is not installed)
# spacy>=3.7.0

# ── Data & ML utilities ───────────────────────────────────────────────────────
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.1.0

# ── API ───────────────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-multipart>=0.0.9

# ── Visualisation / reporting ─────────────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.66.0
PyYAML>=6.0.1
joblib>=1.3.0
